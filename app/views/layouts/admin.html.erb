<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <title>FoodApp - Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Favicon -->
    <%= favicon_link_tag "favicon.ico" %>

    <!-- Tailwind CSS -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <!-- JavaScript -->
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans text-gray-900">
    <main class="flex">
      <!-- Sidebar -->
      <nav class="flex-shrink-0 w-64 h-screen bg-gray-600 sticky top-0">
        <div class="p-4 flex items-center space-x-2">
          <%= image_tag "logo.png", class: 'w-16 h-16' %>
          <h1 class="text-lg font-bold text-gray-50 hidden md:block">FoodApp</h1>
        </div>

        <ul class="flex flex-col mt-4 text-gray-50">
          <li>
            <%= link_to admin_path, class:"flex items-center px-4 py-2 hover:rounded hover:bg-gray-700" do %>
              <%= icon('fa-solid', 'gauge-high', class: "w-5 h-5") %>
              <span class="ml-2 hidden md:inline-block">Dashboard</span>
            <% end %>
          </li>

          <li>
            <%= link_to products_path, class:"flex items-center px-4 py-2 hover:rounded hover:bg-gray-700" do %>
              <%= icon('fa-solid', 'cart-shopping', class: "w-5 h-5") %>
              <span class="ml-2 hidden md:inline-block">Products</span>
            <% end %>
          </li>

          <li>
            <%= link_to categories_path, class:"flex items-center px-4 py-2 hover:rounded hover:bg-gray-700" do %>
              <%= icon('fa-solid', 'list', class: "w-5 h-5") %>
              <span class="ml-2 hidden md:inline-block">Categories</span>
            <% end %>
          </li>

          <li>
            <%= link_to orders_path, class:"flex items-center px-4 py-2 hover:rounded hover:bg-gray-700" do %>
              <%= icon('fa-solid', 'truck-fast', class: "w-5 h-5") %>
              <span class="ml-2 hidden md:inline-block">Orders</span>
            <% end %>
          </li>
        </ul>
      </nav>

      <!-- Main content -->
      <div class="flex-1 p-6">
        <%= yield %>
      </div>
    </main>
  </body>
</html>
